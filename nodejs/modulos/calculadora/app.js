const { soma, subtrai, multiplica, divide } = require('./funcoes_matematicas');

// Exemplos de uso:
console.log('Soma:', soma(5, 3));
console.log('Subtração:', subtrai(10, 4));
console.log('Multiplicação:', multiplica(2, 7));
console.log('Divisão:', divide(20, 5));// Evitando divisão por zero 

